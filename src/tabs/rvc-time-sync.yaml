- id: 0c2fabe1084581af
  type: tab
  label: RVC Time Sync
  disabled: false
  info: >-
    Broadcasts the current system time to the RV-C network every 60 seconds
    using SET_DATE_TIME_COMMAND (DGN 1FFFEh). Controlled by the
    rvc_time_sync_enabled addon config option (default: disabled).
  env: []
- id: bce5d439a67df7da
  type: inject
  z: 0c2fabe1084581af
  name: Every 60s
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: "60"
  crontab: ""
  once: true
  onceDelay: "10"
  topic: ""
  payload: ""
  payloadType: date
  x: 170
  "y": 120
  wires:
    - - dd22c9c987534342
- id: dd22c9c987534342
  type: function
  z: 0c2fabe1084581af
  name: build_time_sync_frame
  func: |
    const timeSyncEnabled = global.get("timeSyncEnabled");
    if (!timeSyncEnabled) return null;

    const PRIORITY = 6;
    const DGN = 0x1FFFE; // SET_DATE_TIME_COMMAND
    const SOURCE_ADDRESS = global.get("rvc_source_address") || 254;

    const dt = new Date();

    const rvcDow = (jsDow) => (jsDow === 0 ? 1 : jsDow + 1);

    const jan = new Date(dt.getFullYear(), 0, 1).getTimezoneOffset();
    const jul = new Date(dt.getFullYear(), 6, 1).getTimezoneOffset();
    const std = Math.max(jan, jul);
    const isDst = dt.getTimezoneOffset() < std;
    const tzCode = isDst ? std / 60 - 1 : std / 60;

    const dataBytes = [
      Math.max(0, Math.min(255, dt.getFullYear() - 2000)),
      dt.getMonth() + 1,
      dt.getDate(),
      rvcDow(dt.getDay()),
      dt.getHours(),
      dt.getMinutes(),
      dt.getSeconds(),
      tzCode & 0xff,
    ];

    const dataHex = dataBytes
      .map((b) => b.toString(16).padStart(2, "0"))
      .join("")
      .toUpperCase();
    const canIdInt = (PRIORITY << 26) | (DGN << 8) | SOURCE_ADDRESS;
    const canIdHex = canIdInt.toString(16).padStart(8, "0").toUpperCase();

    node.status({
      fill: "green",
      shape: "dot",
      text: `${dt.getMonth() + 1}/${dt.getDate()} ${dt.getHours().toString().padStart(2, "0")}:${dt.getMinutes().toString().padStart(2, "0")}:${dt.getSeconds().toString().padStart(2, "0")} tz=${tzCode}`,
    });

    return { topic: "can/send", payload: `${canIdHex}#${dataHex}` };
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 390
  "y": 120
  wires:
    - - de36b6c52fd2b2cc
- id: de36b6c52fd2b2cc
  type: link out
  z: 0c2fabe1084581af
  name: "MQTT out: Retain FALSE"
  mode: link
  links:
    - 74329f13cbbc528a
  x: 615
  "y": 120
  wires: []

- id: 292e70a6ba25b323
  type: tab
  label: Config
  env: []
  normalizedLabel: config
  disabled: false
  info: ""
- id: 012b02175347902e
  type: inject
  z: 292e70a6ba25b323
  name: Load on Start
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: ""
  crontab: ""
  once: true
  onceDelay: ".1"
  topic: ""
  payload: ""
  payloadType: date
  x: 220
  "y": 1300
  g: f81ca31c89243206
  wires:
    - - d6680567062a0908
- id: 027d238476871cfe
  type: link in
  z: 292e70a6ba25b323
  name: CONFIG_GLOBALS
  links:
    - 88e5fa035bedb48c
  x: 1110
  "y": 1780
  g: 9e47041ed71c4494
  l: true
  wires:
    - - 6b1a7191643fa28e
- id: 02bdfc59de71b5a5
  type: link out
  z: 292e70a6ba25b323
  name: STATUS
  mode: link
  links:
    - fbc5efde2c447eda
  x: 1760
  "y": 400
  g: f09f79586501cb0b
  l: true
  wires: []
- id: 03a998942a940736
  type: function
  z: 292e70a6ba25b323
  d: true
  name: tally
  func: |
    // Tally node (temporary diagnostic)
    const counts = flow.get("dgnCounts") || {};
    const name = msg.payload.dgn_name;
    counts[name] = (counts[name] || 0) + 1;
    flow.set("dgnCounts", counts);
    return msg; // pass through unmodified
  outputs: 0
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1150
  "y": 600
  g: ceee39e4b10d6030
  wires: []
- id: 07b8bcce03f19e8f
  type: inject
  z: 292e70a6ba25b323
  name: Device discovery
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: ""
  crontab: ""
  once: true
  onceDelay: "30"
  topic: ""
  payload: ""
  payloadType: date
  x: 1140
  "y": 1020
  g: 8ef86a1c4b59d8e1
  wires:
    - - 73abad3deeed31e8
      - 695ef0ed6d78a244
      - 3f5eb271f702c8f3
      - 1e244bad0304bb74
      - d3cc901dd2fbd359
      - ba0c52ff61838986
      - 8e350e2c0a0536b4
- id: 092c751dcdd12270
  type: function
  z: 292e70a6ba25b323
  name: reset_uniqueUnknown
  func: |
    flow.set("uniqueUnknown", []);
    return null;  // Nothing needs to go downstream
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 2100
  "y": 540
  g: 9dd6a98018824be9
  wires:
    - []
- id: 0a63b31d61802934
  type: link out
  z: 292e70a6ba25b323
  name: "MQTT out: Retain TRUE"
  mode: link
  links:
    - f658418a7b9b4857
  x: 730
  "y": 1300
  g: f81ca31c89243206
  l: true
  wires: []
- id: 0c76b4a89a0e60ca
  type: inject
  z: 292e70a6ba25b323
  name: On start
  props:
    - p: reset
      v: "true"
      vt: bool
  repeat: ""
  crontab: ""
  once: true
  onceDelay: 0.1
  topic: ""
  x: 1120
  "y": 380
  g: f09f79586501cb0b
  wires:
    - - 21dfe84b8adafa6e
- id: 1250cab4e8633d65
  type: group
  z: 292e70a6ba25b323
  name: Recording unknown DGNs
  x: 94
  "y": 1659
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 4bea6d59dec3cdde
    - 880ca1b6bf10b8d2
    - cd73ee739dad9f59
    - cfa55144f4a17a5c
    - dca8346e061f1d42
    - f3ee6dc19b0155a0
    - ff7901a611c1e1aa
- id: 1ac6e0fa1cbb3852
  type: mqtt out
  z: 292e70a6ba25b323
  name: "MQTT Out: Retain FALSE"
  topic: ""
  qos: "0"
  retain: "false"
  respTopic: ""
  contentType: ""
  userProps: ""
  correl: ""
  expiry: ""
  broker: 80727e60a251c36c
  x: 630
  "y": 600
  g: 3c819b7900145438
  wires: []
- id: 1becc1dacb0ecaf8
  type: function
  z: 292e70a6ba25b323
  name: geo_handle_toggle
  func: |
    // Handles enable/disable of Geo integration via addon config
    // Input: msg from librecoach/config/geo_enabled ("true" / "false")
    // Output 1 → MQTT Out (entity deletion on disable)

    // Only handle geo_enabled messages
    const key = msg.topic.split("/").pop();
    if (key !== "geo_enabled") return null;

    const enabled = msg.payload.toString() === "true";

    if (enabled) {
      node.status({ fill: "green", shape: "dot", text: "Enabled" });
      return null;
    }

    // === Disable: delete all tracked entities from HA ===
    // Only need to delete what we create in status_geo.js
    const sensors = ["city", "state", "timezone", "elevation"];

    let deletedCount = 0;
    sensors.forEach((sensor) => {
      const entityId = `geo_${sensor}`;
      node.send({ topic: `homeassistant/sensor/${entityId}/config`, payload: "" });
      deletedCount++;
    });

    // Clear persisted tracking state
    flow.set("geoSensorsCreated", false);

    node.status({
      fill: "red",
      text: `Disabled — removed ${deletedCount} entities`,
    });

    return null;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 450
  "y": 1480
  g: f67b4a374db41b27
  wires:
    - - 5ffb64e684c20ea8
- id: 1e244bad0304bb74
  type: link out
  z: 292e70a6ba25b323
  name: Clear unique values
  mode: link
  links:
    - 893d80d1125057e8
    - 2343c8c12f2081fa
    - a58b5f05c8346eab
  x: 1420
  "y": 1160
  g: 8ef86a1c4b59d8e1
  l: true
  wires: []
- id: 21029b71ed58348c
  type: mqtt in
  z: 292e70a6ba25b323
  name: Config in
  topic: librecoach/config/#
  qos: "1"
  datatype: auto
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 180
  "y": 680
  g: 3c819b7900145438
  wires:
    - - c9d298bbaa855984
- id: 2110ea4d0b85f784
  type: mqtt in
  z: 292e70a6ba25b323
  name: CAN in
  topic: can/raw
  qos: "1"
  datatype: auto-detect
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 930
  "y": 460
  g: f09f79586501cb0b
  wires:
    - - 8d7d5d3ea117cf79
- id: 21dfe84b8adafa6e
  type: rbe
  z: 292e70a6ba25b323
  name: ""
  func: rbe
  gap: ""
  start: ""
  inout: out
  septopics: true
  property: payload
  topi: filter_key
  x: 1290
  "y": 460
  g: f09f79586501cb0b
  wires:
    - - a6594a9464d8fe4e
      - 3935564c9401e95d
- id: 375ced2f0a80dc5c
  type: note
  z: 292e70a6ba25b323
  content: |-
    # Config
    System configuration and initialization. Loads reference files,
    claims an address on the CAN bus, stores user option settings,
    and sets up the global state that all other flows depend on.
  bgcolor: "#f3e5ab"
  textcolor: "#333333"
  x: 330
  "y": 124
  wires: []
- id: 3935564c9401e95d
  type: function
  z: 292e70a6ba25b323
  name: record_unknown_capture
  func: >
    // Record Unknown CAN — Terminal Capture Sink

    // Sits AFTER the routing switch at the end of the line.

    // Only receives DGNs routed to it (UNKNOWN, PROPRIETARY, COMMAND, STATUS).

    // Auto-stop: 2 minutes elapsed OR 1000 messages captured

    // Output 1: MQTT state (auto-stop off message)

    // Output 2: Changed messages (debug passthrough)


    const MAX_MESSAGES = 1000;

    const MAX_DURATION_MS = 10 * 60 * 1000;

    const TIMEZONE = "America/New_York";


    // STATUS DGNs allowed through — discrete state only, no analog sensors.

    // COMMAND, UNKNOWN, and PROPRIETARY always pass without this check.

    const STATUS_WHITELIST = [
      "AC_LOAD_STATUS",
      "AC_LOAD_STATUS_2",
      "AAS_STATUS",
      "AAS_SENSOR_STATUS",
      "AIR_CONDITIONER_STATUS",
      "HEAT_PUMP_STATUS",
      "AUTOFILL_STATUS",
      "AWNING_STATUS",
      "AWNING_STATUS_2",
      "CHASSIS_MOBILITY_STATUS",
      "CHASSIS_MOBILITY_STATUS_2",
      "CIRCULATION_PUMP_STATUS",
      "DC_DIMMER_STATUS_1",
      "DC_DIMMER_STATUS_2",
      "DC_DIMMER_STATUS_3",
      "DC_DISCONNECT_STATUS",
      "DC_LIGHTING_CONTROLLER_STATUS_1",
      "DC_LIGHTING_CONTROLLER_STATUS_2",
      "DC_LIGHTING_CONTROLLER_STATUS_3",
      "DC_LIGHTING_CONTROLLER_STATUS_4",
      "DC_LIGHTING_CONTROLLER_STATUS_5",
      "DC_LIGHTING_CONTROLLER_STATUS_6",
      "DC_LOAD_STATUS",
      "DC_LOAD_STATUS_2",
      "DC_MOTOR_CONTROL_STATUS",
      "DIGITAL_INPUT_STATUS",
      "FLOOR_HEAT_STATUS",
      "FURNACE_STATUS",
      "GAS_SENSOR_STATUS",
      "GENERATOR_STATUS_1",
      "GENERATOR_STATUS_2",
      "GENERIC_INDICATOR_STATUS",
      "HYDRAULIC_PUMP_STATUS",
      "INVERTER_STATUS",
      "LOCK_STATUS",
      "LEVELING_CONTROL_STATUS",
      "LEVELING_JACK_STATUS",
      "LEVELING_AIR_STATUS",
      "REFRIGERATOR_STATUS",
      "ROOF_FAN_STATUS_1",
      "ROOF_FAN_STATUS_2",
      "SLIDE_STATUS",
      "SLIDE_MOTOR_STATUS",
      "STEP_STATUS",
      "THERMOSTAT_STATUS_1",
      "THERMOSTAT_STATUS_2",
      "TV_LIFT_STATUS",
      "ATS_STATUS",
      "VALVE_STATUS",
      "VEHICLE_SEAT_STATUS",
      "VEHICLE_SEAT_LIGHTING_STATUS",
      "WATER_PUMP_STATUS",
      "WATERHEATER_STATUS",
      "WATERHEATER_STATUS_2",
      "WINDOW_STATUS",
      "WINDOW_SHADE_CONTROL_STATUS",
    ];


    // --- STATUS whitelist check ---

    // COMMAND, UNKNOWN, PROPRIETARY always pass. Known STATUS must be
    whitelisted.

    const dgn_name = msg.payload.dgn_name;

    if (
      dgn_name !== "UNKNOWN" &&
      dgn_name !== "PROPRIETARY" &&
      !dgn_name.includes("COMMAND") &&
      !STATUS_WHITELIST.includes(dgn_name)
    ) {
      return [null, null];
    }


    // --- Parse data bytes from hex string ---

    const raw = msg.payload.originalMessage || "";

    const dataHex = msg.payload.data_payload || "";

    const byteHexPairs = dataHex.match(/.{1,2}/g) || [];

    const instanceId =
      byteHexPairs.length > 0 ? parseInt(byteHexPairs[0], 16) : null;
    const data = byteHexPairs.map((h) => h.toUpperCase()).join(" ");


    const seenKey = `${msg.payload.dgn}:${instanceId}`;


    // RBE node upstream handles deduplication.

    // Any message reaching this node is considered a legitimate event to
    capture.

    const changed = true;


    // Pass all received messages to output 2 for debug

    const debugMsg = { payload: { seenKey, data, dgn_name } };


    // Gate on recording state

    const recording = global.get("recordUnknown", "file");

    if (!recording) {
      node.status({ fill: "grey", shape: "ring", text: "Stopped" });
      return [null, debugMsg];
    }

    if (!changed) {
      const curLog = global.get("recordUnknownLog", "file") || [];
      node.status({
        fill: "red",
        shape: "ring",
        text: `Watching... ${curLog.length}`,
      });
      return [null, null];
    }


    // --- Timing ---

    const log = global.get("recordUnknownLog", "file") || [];

    const startTime = global.get("recordUnknownStart", "file") || Date.now();

    const elapsed = Date.now() - startTime;


    // Check auto-stop before adding

    if (log.length >= MAX_MESSAGES || elapsed >= MAX_DURATION_MS) {
      global.set("recordUnknown", false, "file");

      const reason =
        log.length >= MAX_MESSAGES ? `${MAX_MESSAGES} messages` : "10 min elapsed";
      node.status({
        fill: "yellow",
        shape: "ring",
        text: `Auto-stopped — ${reason}`,
      });

      const offMsg = {
        topic: "homeassistant/switch/librecoach_record_unknown/state",
        payload: "0",
        retain: true,
      };

      return [offMsg, debugMsg];
    }


    // --- Local timestamp ---

    const localISO = new Date()
      .toLocaleString("sv-SE", { timeZone: TIMEZONE, hour12: false })
      .replace(" ", "T");

    // --- Build log entry ---

    log.push({
      timestamp: localISO,
      dgn: msg.payload.dgn,
      dgn_name: dgn_name,
      instance: instanceId !== null ? instanceId : null,
      data: data,
      raw_can: raw,
    });

    global.set("recordUnknownLog", log, "file");


    node.status({
      fill: "red",
      shape: "dot",
      text: `Recording... ${log.length}`,
    });


    return [null, debugMsg];
  outputs: 2
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1530
  "y": 300
  g: f09f79586501cb0b
  wires:
    - - 6376d98589368f2b
    - - d79041ca83ce8b8c
- id: 398683928338f9c6
  type: link out
  z: 292e70a6ba25b323
  name: AQUAHOT
  mode: link
  links:
    - 00b1afc9a96aca8e
    - cd5a041811bc2c9a
  x: 1770
  "y": 360
  g: f09f79586501cb0b
  l: true
  wires: []
- id: 3c819b7900145438
  type: group
  z: 292e70a6ba25b323
  name: MQTT
  x: 94
  "y": 239
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 1ac6e0fa1cbb3852
    - 21029b71ed58348c
    - 54cd7e4a8959a77c
    - 64c5e5cfd4382453
    - 74329f13cbbc528a
    - 88e5fa035bedb48c
    - 9e9b11b9441e51db
    - b86f82e84d961549
    - c9d298bbaa855984
    - cacf35b859af22a9
    - d34a8c1fd2cc4320
    - f658418a7b9b4857
- id: 3d07bdca6b71b669
  type: function
  z: 292e70a6ba25b323
  d: true
  name: tally_report
  func: node.warn(JSON.stringify(flow.get("dgnCounts")))
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1150
  "y": 660
  g: ceee39e4b10d6030
  wires:
    - []
- id: 3e9c6f1a2b4d5e6f
  type: inject
  z: 292e70a6ba25b323
  name: Flows loaded
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: ""
  crontab: ""
  once: true
  onceDelay: "10"
  topic: ""
  payload: ""
  payloadType: date
  x: 220
  "y": 1380
  wires:
    - - 9d8e7f6a5b4c3d2e
- id: 3f5eb271f702c8f3
  type: function
  z: 292e70a6ba25b323
  name: poll_shades
  func: >-
    // Polling Function for Shades

    // Requests WINDOW_SHADE_CONTROL_STATUS (1FEDE)


    // --- Configuration ---

    // The source address of this Node-RED system.

    const SOURCE_ADDRESS = global.get("rvc_source_address") || 254; // 0xFE

    const PRIORITY = 6;       // Standard priority for requests (Included in
    18EAFF)

    const DGN = "18EAFF";     // DGN for a broadcast request message


    // The DGN for WINDOW_SHADE_CONTROL_STATUS

    const REQUESTED_DGN = "01FEDE";


    // Build the data payload

    // The payload is the DGN being requested, sent in 3-byte little-endian
    format.

    // The remaining 5 bytes are padded with 0xFF.

    const requestedDgnInt = parseInt(REQUESTED_DGN, 16);

    const dataPayloadBytes = new Uint8Array(8);

    dataPayloadBytes.fill(0xFF); // Pad the entire payload with 0xFF first

    dataPayloadBytes[0] = requestedDgnInt & 0xFF;         // Byte 1: DE

    dataPayloadBytes[1] = (requestedDgnInt >> 8) & 0xFF;  // Byte 2: FE

    dataPayloadBytes[2] = (requestedDgnInt >> 16) & 0xFF; // Byte 3: 01

    const data_payload_hex = Array.from(dataPayloadBytes).map(b =>
    b.toString(16).padStart(2, '0')).join('');


    // Construct the CAN ID

    // Combine the DGN (which already includes Priority 6 and PGN 0xEAFF) with
    the Source Address.

    const dgnInt = parseInt(DGN, 16);

    const canIdInt = (dgnInt << 8) | SOURCE_ADDRESS;

    const canIdHex = canIdInt.toString(16).padStart(8, '0');


    // Construct the final output message in CAN bus format

    msg.topic = "can/send";

    msg.payload = `${canIdHex.toUpperCase()}#${data_payload_hex.toUpperCase()}`;


    // node.warn(`[Poll Shades] Sending global request for shade status. CAN:
    ${msg.payload}`);


    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1390
  "y": 1000
  g: 8ef86a1c4b59d8e1
  wires:
    - - a2334b77d3764815
- id: 45414bacecada477
  type: link out
  z: 292e70a6ba25b323
  name: ADDRESS_CLAIMED
  mode: link
  links:
    - f01db5ebc3ae2e18
  x: 1800
  "y": 680
  g: f09f79586501cb0b
  l: true
  wires: []
- id: 4bea6d59dec3cdde
  type: mqtt in
  z: 292e70a6ba25b323
  name: Recording trigger
  topic: librecoach/export/unknown/trigger
  qos: "1"
  datatype: utf8
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 200
  "y": 1820
  g: 1250cab4e8633d65
  wires:
    - - f3ee6dc19b0155a0
- id: 4e46ba83df00a31b
  type: function
  z: 292e70a6ba25b323
  name: address_claim_monitor
  func: |-
    // RV-C Address Claim Monitor Function
    // Assumes it ONLY receives decoded ADDRESS_CLAIMED messages (DGN EE00)
    // Compares device NAMEs to determine winner in case of conflict

    const claimInProgress = flow.get("claim_in_progress");
    if (!claimInProgress) {
      return null; // Not in the claiming process
    }

    const claimingAddress = flow.get("claiming_address");
    const ourDeviceName = flow.get("our_device_name");
    const competitorName = msg.payload.data_payload.toUpperCase();
    const sourceAddressInt = parseInt(msg.payload.sourceAddress, 16);

    // Check if a message is for the same address we are trying to claim
    if (sourceAddressInt === claimingAddress) {
      // First, verify this isn't our own message being echoed back.
      // If the NAMEs are identical, it's our own claim. Ignore it and let the timer run.
      if (competitorName === ourDeviceName) {
        // This is our own message, not a real conflict.
        return null;
      }

      // If we get here, it's a real conflict from a different device.
      node.warn(`Address ${claimingAddress} conflict detected!`);
      node.warn(`Our NAME: ${ourDeviceName}, Competitor NAME: ${competitorName}`);

      // Compare NAMEs - Lower numerical value wins
      const ourNameValue = BigInt("0x" + ourDeviceName);
      const competitorNameValue = BigInt("0x" + competitorName);

      if (ourNameValue < competitorNameValue) {
        // WE WIN
        node.warn(
          `We WIN the conflict. Continuing to claim address ${claimingAddress}`,
        );
        return null;
      } else {
        // WE LOSE
        node.warn(
          `We LOSE the conflict. Cancelling timer and trying next address.`,
        );
        flow.set("claim_in_progress", false);

        const retryMsg = { topic: "address_claim_retry" };
        const resetTimerMsg = { reset: true };

        return [retryMsg, resetTimerMsg];
      }
    }

    // If we reach here, it's a claim for a different address, so ignore it.
    return null;
  outputs: 2
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 470
  "y": 920
  g: 8ef86a1c4b59d8e1
  wires:
    - - b846bc1605dae821
    - - 55071115a3fe7c2c
- id: 4fa9b9fdc36c83da
  type: link out
  z: 292e70a6ba25b323
  name: COMMAND
  mode: link
  links:
    - 00b1afc9a96aca8e
  x: 1770
  "y": 440
  g: f09f79586501cb0b
  l: true
  wires: []
- id: 50dfcea54d80866b
  type: mqtt in
  z: 292e70a6ba25b323
  name: Location updates
  topic: can/status/geo
  qos: "2"
  datatype: json
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 200
  "y": 1560
  g: f67b4a374db41b27
  wires:
    - - a8a757fe3b31cf9c
- id: 515386b6b0d01893
  type: inject
  z: 292e70a6ba25b323
  name: Load 5 seconds after Start
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: ""
  crontab: ""
  once: true
  onceDelay: "5"
  topic: ""
  payload: ""
  payloadType: date
  x: 260
  "y": 840
  g: 8ef86a1c4b59d8e1
  wires:
    - - b846bc1605dae821
- id: 5169613bd2735138
  type: group
  z: 292e70a6ba25b323
  name: RV-C Time Sync
  x: 994
  "y": 1499
  style:
    stroke: "#777777"
    label: true
  nodes:
    - bce5d439a67df7da
    - c83c6b2b70c927a3
    - dd22c9c987534342
- id: 52f6bcc4fb0ca263
  type: inject
  z: 292e70a6ba25b323
  name: Reset
  props:
    - p: reset
      v: "true"
      vt: bool
  repeat: ""
  crontab: ""
  once: false
  onceDelay: 0.1
  topic: ""
  x: 1130
  "y": 340
  g: f09f79586501cb0b
  wires:
    - - 21dfe84b8adafa6e
- id: 54cd7e4a8959a77c
  type: mqtt in
  z: 292e70a6ba25b323
  name: HA in
  topic: homeassistant/climate/+/set_preset_mode
  qos: "1"
  datatype: utf8
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 170
  "y": 400
  g: 3c819b7900145438
  wires:
    - - 64c5e5cfd4382453
      - d34a8c1fd2cc4320
- id: 55071115a3fe7c2c
  type: delay
  z: 292e70a6ba25b323
  name: ""
  pauseType: delay
  timeout: "250"
  timeoutUnits: milliseconds
  rate: "1"
  nbRateUnits: "1"
  rateUnits: second
  randomFirst: "1"
  randomLast: "5"
  randomUnits: seconds
  drop: false
  allowrate: false
  outputs: 1
  x: 890
  "y": 920
  g: 8ef86a1c4b59d8e1
  wires:
    - - 6423c433e099d2a5
- id: 5ffb64e684c20ea8
  type: link out
  z: 292e70a6ba25b323
  name: "MQTT out: Retain TRUE"
  mode: link
  links:
    - f658418a7b9b4857
  x: 730
  "y": 1500
  g: f67b4a374db41b27
  l: true
  wires: []
- id: 6376d98589368f2b
  type: link out
  z: 292e70a6ba25b323
  name: "MQTT out: Retain TRUE"
  mode: link
  links:
    - f658418a7b9b4857
  x: 1810
  "y": 280
  g: f09f79586501cb0b
  l: true
  wires: []
- id: 6423c433e099d2a5
  type: function
  z: 292e70a6ba25b323
  name: address_claim_success
  func: >
    // RV-C Address Claim Success Function

    // Called after 250ms timer expires - finalizes address claim if no conflict
    lost

    // Reference: RV-C Spec Section 3.3.2 - Source Address Claiming

    //

    // IMPORTANT: This does NOT send a second ADDRESS_CLAIMED message.

    // We already sent ADDRESS_CLAIMED at the start of the process.

    // Success = 250ms timer expires without losing a conflict.


    // Get the address we were trying to claim when this timer started

    const claimedAddress = msg.claiming_address;


    if (claimedAddress === undefined || claimedAddress === null) {
      node.error("No address to claim - timer message missing claiming_address");
      return null;
    }


    // Check if this timer is still valid (claim hasn't been cancelled by
    monitor)

    const claimInProgress = flow.get("claim_in_progress");

    if (!claimInProgress) {
      // Claim was cancelled during the 250ms window (we lost a conflict)
      node.warn(
        `Timer expired for address ${claimedAddress}, but claim was cancelled - ignoring`,
      );
      return null;
    }


    // Verify we're still trying to claim the same address

    const currentClaimingAddress = flow.get("claiming_address");

    if (currentClaimingAddress !== claimedAddress) {
      node.warn(
        `Timer expired for address ${claimedAddress}, but we're now trying ${currentClaimingAddress} - ignoring stale timer`,
      );
      return null;
    }


    // SUCCESS! No conflict detected during the 250ms monitoring window

    // Store the claimed address to global context

    global.set("rvc_source_address", claimedAddress);


    // Clear claiming flags

    flow.set("claim_in_progress", false);

    flow.set("claiming_address", null);

    flow.set("our_device_name", null);


    //node.warn(`Successfully claimed address ${claimedAddress}
    (0x${claimedAddress.toString(16).toUpperCase()})`);

    //node.warn(`Address stored to global.rvc_source_address:
    ${claimedAddress}`);


    node.log(
      `Successfully claimed address ${claimedAddress} (0x${claimedAddress.toString(16).toUpperCase()})`,
    );


    // Return msg to trigger downstream polling

    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1110
  "y": 920
  g: 8ef86a1c4b59d8e1
  wires:
    - - a2334b77d3764815
      - 73abad3deeed31e8
      - 695ef0ed6d78a244
      - 3f5eb271f702c8f3
      - 1e244bad0304bb74
- id: 64c5e5cfd4382453
  type: link out
  z: 292e70a6ba25b323
  name: HA in
  mode: link
  links:
    - a3b957142479c075
  x: 570
  "y": 380
  g: 3c819b7900145438
  l: true
  wires: []
- id: 695ef0ed6d78a244
  type: function
  z: 292e70a6ba25b323
  name: poll_locks
  func: >
    // Polling Function for Locks

    // Requests LOCK_STATUS (1FEE5)


    // --- Configuration ---

    // The source address of this Node-RED system.

    const SOURCE_ADDRESS = global.get("rvc_source_address") || 254;

    const PRIORITY = 6;       // Standard priority for requests

    const DGN = "18EAFF";     // DGN for a broadcast request message


    // Build the data payload

    // This is the DGN for LOCK_STATUS (1FEE5), sent in little-endian format
    (least-significant byte first).

    const data_payload = "E5FE01FFFFFFFF";


    // Construct the CAN ID

    const dgnInt = parseInt(DGN, 16);

    const canIdInt = (dgnInt << 8) | SOURCE_ADDRESS;

    const canIdHex = canIdInt.toString(16).padStart(8, '0');


    // Construct the final output message in CAN bus format

    msg.topic = "can/send";

    msg.payload = `${canIdHex.toUpperCase()}#${data_payload.toUpperCase()}`;


    //node.warn(`[Poll Locks] Sending global request for lock status. CAN:
    ${msg.payload}`);


    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1380
  "y": 960
  g: 8ef86a1c4b59d8e1
  wires:
    - - a2334b77d3764815
- id: 6b1a7191643fa28e
  type: function
  z: 292e70a6ba25b323
  name: beta_handle_toggle
  func: >-
    // Handles enable/disable of Beta features and tracks beta entities for
    automatic deletion.

    // Input can be both config trigger and MQTT discovery configs.

    // Output 1 → MQTT Out (entity creation configs or entity deletion payloads)

    // Output 2 → Filter nodes (reset on enable)


    // 1. Handle config enable/disable messages

    const key = msg.topic.split("/").pop();

    if (key === "beta_enabled") {
      const enabled = msg.payload.toString() === "true";

      if (enabled) {
        node.status({ fill: "green", shape: "dot", text: "Enabled" });
        // Output 2 reset for filter nodes
        return [null, { reset: true }];
      }

      // === Disable: delete all tracked beta entities from HA ===
      const discoveryTopics = global.get("betaDiscoveryTopics", "file") || [];

      if (discoveryTopics.length === 0) {
        node.status({
          fill: "yellow",
          shape: "ring",
          text: "Disabled (no entities)",
        });
        return [null, null];
      }

      // Send deletion messages to output 1 (MQTT Out)
      const msgs = discoveryTopics.map((topic) => ({ topic: topic, payload: "" }));

      // Clear persisted tracking state
      global.set("betaDiscoveryTopics", [], "file");

      node.status({
        fill: "red",
        text: `Disabled — removed ${discoveryTopics.length} entities`,
      });

      return [msgs, null];
    }


    // 2. Handle incoming discovery config messages (from beta Create nodes)

    if (
      msg.topic &&
      msg.topic.startsWith("homeassistant/") &&
      msg.topic.endsWith("/config")
    ) {
      const isBetaEnabled = global.get("betaEnabled");

      // Only process if beta is actually enabled
      if (isBetaEnabled === true) {
        let discoveryTopics = global.get("betaDiscoveryTopics", "file") || [];

        // Add to tracked list if not already there
        if (!discoveryTopics.includes(msg.topic)) {
          discoveryTopics.push(msg.topic);
          global.set("betaDiscoveryTopics", discoveryTopics, "file");
          node.status({
            fill: "green",
            shape: "dot",
            text: `Tracking ${discoveryTopics.length} entities`,
          });
        }

        // Pass the message through to Output 1 (MQTT Out) to actually create the entity
        return [msg, null];
      } else {
        // If a create message arrives while beta is disabled, block it.
        return [null, null];
      }
    }


    // Ignore unknown messages

    return [null, null];
  outputs: 2
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1410
  "y": 1780
  g: 9e47041ed71c4494
  wires:
    - - e38f32a1f6d36bfb
    - []
- id: 73abad3deeed31e8
  type: function
  z: 292e70a6ba25b323
  name: poll_dc_dimmer_3
  func: >
    // Polling Function for DC Dimmer

    // Requests DC_DIMMER_STATUS_3 (1FEDA)


    // --- Configuration ---

    // The source address of this Node-RED system.

    const SOURCE_ADDRESS = global.get("rvc_source_address") || 254;

    const PRIORITY = 6; // Standard priority for requests

    const DGN = "18EAFF"; // DGN for a broadcast request message


    // Build the data payload

    // This is the DGN for DC_DIMMER_STATUS_3 (1FEDA), sent in little-endian
    format (least-significant byte first).

    const data_payload = "DAFE01FFFFFFFF";


    // Construct the CAN ID

    const dgnInt = parseInt(DGN, 16);

    const canIdInt = (dgnInt << 8) | SOURCE_ADDRESS;

    const canIdHex = canIdInt.toString(16).padStart(8, "0");


    // Construct the final output message in CAN bus format

    msg.topic = "can/send";

    msg.payload = `${canIdHex.toUpperCase()}#${data_payload.toUpperCase()}`;


    node.log(`Polling started`);


    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1410
  "y": 920
  g: 8ef86a1c4b59d8e1
  wires:
    - - a2334b77d3764815
- id: 74329f13cbbc528a
  type: link in
  z: 292e70a6ba25b323
  name: "MQTT out: Retain FALSE"
  links:
    - a2334b77d3764815
    - 6aa079d768e1571c
    - 52b505b69f88888d
    - d95299a44e1d7ed5
    - c83c6b2b70c927a3
  x: 230
  "y": 600
  g: 3c819b7900145438
  l: true
  wires:
    - - 1ac6e0fa1cbb3852
- id: 74c7e4a6cc2508eb
  type: file in
  z: 292e70a6ba25b323
  name: Read DGN table
  filename: /config/projects/librecoach-node-red/data/dgn-summary-table.json
  filenameType: str
  format: utf8
  chunk: false
  sendError: false
  allProps: false
  x: 1320
  "y": 1420
  g: 9cfcca5efde78052
  wires:
    - - c78420a075d43408
- id: 7e609140970f546c
  type: inject
  z: 292e70a6ba25b323
  name: Load on Start
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: ""
  crontab: ""
  once: true
  onceDelay: "0.1"
  topic: ""
  payload: ""
  payloadType: date
  x: 1120
  "y": 1420
  g: 9cfcca5efde78052
  wires:
    - - 74c7e4a6cc2508eb
- id: 8037e09fbf4ac87b
  type: inject
  z: 292e70a6ba25b323
  d: true
  name: ""
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: ""
  crontab: ""
  once: false
  onceDelay: 0.1
  topic: ""
  payload: ""
  payloadType: date
  x: 980
  "y": 660
  g: ceee39e4b10d6030
  wires:
    - - 3d07bdca6b71b669
- id: 8337a7f0dd0435d6
  type: http request
  z: 292e70a6ba25b323
  name: ""
  method: use
  ret: obj
  paytoqs: ignore
  url: ""
  tls: ""
  persist: false
  proxy: ""
  insecureHTTPParser: false
  authType: ""
  senderr: false
  headers: []
  x: 1550
  "y": 1300
  g: efe52fc701f550cf
  wires:
    - []
- id: 880ca1b6bf10b8d2
  type: function
  z: 292e70a6ba25b323
  name: record_unknown_toggle
  func: |
    // Record Unknown CAN — Toggle Handler
    // Input: MQTT payload "ON" or "OFF" from HA switch
    // On start: clears log, sets start time, sets recording flag
    // On stop: clears flag, shows message count
    // Output: retained state publish back to HA

    const cmd = msg.payload;

    if (cmd === "1") {
      global.set("recordUnknown", true, "file");
      global.set("recordUnknownStart", Date.now(), "file");
      global.set("recordUnknownLog", [], "file");

      node.status({ fill: "red", shape: "dot", text: "Recording..." });

      return {
        topic: "homeassistant/switch/librecoach_record_unknown/state",
        payload: "1",
        retain: true,
      };
    }

    if (cmd === "0") {
      global.set("recordUnknown", false, "file");

      const log = global.get("recordUnknownLog", "file") || [];
      node.status({
        fill: "green",
        shape: "dot",
        text: `Stopped — ${log.length} messages`,
      });

      return {
        topic: "homeassistant/switch/librecoach_record_unknown/state",
        payload: "0",
        retain: true,
      };
    }

    return null;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 450
  "y": 1700
  g: 1250cab4e8633d65
  wires:
    - - dca8346e061f1d42
- id: 88e5fa035bedb48c
  type: link out
  z: 292e70a6ba25b323
  name: CONFIG_GLOBALS
  mode: link
  links:
    - 8fdd1d506d28b9b6
    - bb154cf7301d6fa7
    - 027d238476871cfe
    - ac17a780a9a1dbcf
  x: 620
  "y": 680
  g: 3c819b7900145438
  l: true
  wires: []
- id: 8d7d5d3ea117cf79
  type: function
  z: 292e70a6ba25b323
  name: decode_rvc_can
  func: >
    // RV-C CAN Message Parser

    // Decodes a raw CAN message to output only the fields needed for

    // downstream routing (dgn_name) and decoding (data_payload).


    const originalMessage = msg.payload;


    // Validate input

    if (!originalMessage || typeof originalMessage !== "string") {
      node.warn(
        "Invalid message payload: expected string, got " + typeof originalMessage,
      );
      return null;
    }

    const dgnMap = global.get("dgnMap");

    if (!dgnMap) {
      node.error(
        "dgnMap not found in global context. Ensure the map is initialized.",
      );
      return null;
    }


    // Parse CAN message format: CANID#PAYLOAD

    const parts = originalMessage.split("#");

    if (parts.length !== 2) {
      node.warn("Invalid CAN message format: " + originalMessage);
      return null;
    }

    const canIdHex = parts[0];

    const data_payload = parts[1];

    const canIdNum = parseInt(canIdHex, 16);


    if (isNaN(canIdNum)) {
      node.warn("Invalid CAN ID: " + canIdHex);
      return null;
    }


    // Extract PGN (4-digit) and DGN (5-digit) keys


    // Get the 4-digit J1939 PGN (PF + PS). This value is consistent.

    const pf = (canIdNum >> 16) & 0xff; // PDU Format (Bits 16-23)

    const ps = (canIdNum >> 8) & 0xff; // PDU Specific (Bits 8-15)

    let pgn = ((pf << 8) | ps).toString(16).toUpperCase();


    // Ensure 4 characters for known PGNs (e.g., EF4D)

    while (pgn.length < 4) {
      pgn = "0" + pgn;
    }


    // PDU1 handling: For PF < 240, PS is destination address, not part of DGN

    // Replace PS with 00 for lookup (with exceptions)

    let lookupPgn = pgn;

    if (pf < 0xf0) {
      // PDU1 format
      if (pf === 0xef) {
        // Exception: Proprietary messages keep PS (EF4D, EF64, EF9F)
        lookupPgn = pgn;
      } else if (pf === 0xec || pf === 0xeb) {
        // Exception: Multi-packet transport uses FF
        lookupPgn = pf.toString(16).toUpperCase() + "FF";
      } else {
        // Standard PDU1: Replace PS with 00
        lookupPgn = pf.toString(16).toUpperCase() + "00";
      }
      // Ensure 4 characters
      while (lookupPgn.length < 4) {
        lookupPgn = "0" + lookupPgn;
      }
    }


    // The lookup DGN starts as the lookup PGN

    let dgn = lookupPgn;

    let dgn_name;


    // --- DGN Conversion and Primary Lookup ---

    // Extract Data Page (DP) bit from CAN ID (bit 24)

    const dp = (canIdNum >> 24) & 1;

    if (dp) {
      dgn = "1" + lookupPgn;
    }

    dgn_name = dgnMap.get(dgn);


    // For PDU1 messages (PF < 0xF0), PS is the destination address (DSA)

    let destination_address = null;

    if (pf < 0xf0 && pf !== 0xef && pf !== 0xec && pf !== 0xeb) {
      destination_address = ps;
    }


    // Fallback: some PDU2 devices transmit with DP=0 instead of DP=1
    (non-conformant).

    // If DP=0 lookup failed for a PDU2 message, try the DP=1 variant from the
    table.

    if (!dgn_name && dp === 0 && pf >= 0xf0) {
      const altDgn = "1" + lookupPgn;
      const altName = dgnMap.get(altDgn);
      if (altName) {
        dgn = altDgn;
        dgn_name = altName;
      }
    }


    // Fallback logic for proprietary PGNs not in lookup table

    if (!dgn_name) {
      if (pgn === "EF64") {
        dgn_name = "AQUAHOT_COMMAND_1";
      } else if (pgn === "EF9F") {
        dgn_name = "AQUAHOT_STATUS_1";
      } else if (pgn === "EF4D") {
        dgn_name = "AQUAHOT_THERMOSTAT_STATUS_1";
      } else if (lookupPgn === "6F00") {
        dgn_name = "AQUAHOT_UNUSED";
      } else if (lookupPgn === "6C00") {
        dgn_name = "AQUAHOT_STATUS_2";
      } else if (lookupPgn === "FF01") {
        dgn_name = "AQUAHOT_THERMOSTAT_STATUS_2";
      } else if (lookupPgn === "FF2E") {
        dgn_name = "AQUAHOT_SYSTEM_STATUS_2";
      } else if (lookupPgn === "FF2F") {
        dgn_name = "AQUAHOT_COMMAND_2";
      } else if (lookupPgn === "BF00") {
        dgn_name = "WIRELESS_PANEL_SIGNAL_STATUS";
      } else if (dgn === "1AA00") {
        dgn_name = "WIRELESS_PANEL_QUALITY_STATUS";
      } else if (pgn.startsWith("EF")) {
        dgn_name = "AQUAHOT_UNUSED";
      }
    }


    // Final output

    if (!dgn_name) {
      dgn_name = "UNKNOWN";
    }


    const singleInstanceDgns = {
      WATER_PUMP_STATUS: "water_pump",
      AUTOFILL_STATUS: "autofill",
      GENERATOR_STATUS_1: "generator",
      GENERATOR_STATUS_2: "generator",
      GENERATOR_DEMAND_STATUS: "generator",
      LEVELING_CONTROL_STATUS: "leveling_control",
      WASTEDUMP_STATUS: "wastedump",
    };


    // These DGNs are defined by RV-C as single-instance devices; byte 0 is a

    // status/command bit field, not a device instance.

    const instance =
      singleInstanceDgns[dgn_name] || parseInt(data_payload.substring(0, 2), 16);

    msg.payload = {
      originalMessage: originalMessage,
      dgn: dgn,
      dgn_name: dgn_name,
      instance: instance,
      destination_address: destination_address,
      data_payload: data_payload,
    };


    msg.filter_key = `${dgn_name}_${instance}`;


    // Bypass RBE/deduplication exclusively for DC_DIMMER_COMMAND_2 "Toggle"
    buttons.

    // Byte 3 equals: 0x05 (Toggle). Other commands will naturally change byte
    values.

    if (dgn_name === "DC_DIMMER_COMMAND_2" && data_payload.length >= 8) {
      const cmdByte = parseInt(data_payload.substring(6, 8), 16);
      if (cmdByte === 0x05) {
        msg.payload._ts = Date.now();
      }
    }


    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1100
  "y": 460
  g: f09f79586501cb0b
  wires:
    - - 21dfe84b8adafa6e
- id: 8e350e2c0a0536b4
  type: function
  z: 292e70a6ba25b323
  name: poll_dc_load
  func: |
    // Polling Function for DC Load
    // Requests DC_LOAD_STATUS (1FFBD)

    // --- Configuration ---
    const SOURCE_ADDRESS = global.get("rvc_source_address") || 254;
    const PRIORITY = 6;
    const DGN = "18EAFF";

    // DC_LOAD_STATUS DGN 1FFBD in little-endian: BD FF 01
    const data_payload = "BDFF01FFFFFFFF";

    // Construct the CAN ID
    const dgnInt = parseInt(DGN, 16);
    const canIdInt = (PRIORITY << 26) | (dgnInt << 8) | SOURCE_ADDRESS;
    const canIdHex = canIdInt.toString(16).padStart(8, "0");

    msg.topic = "can/send";
    msg.payload = `${canIdHex.toUpperCase()}#${data_payload.toUpperCase()}`;

    node.log(`Polling DC Load status`);

    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1390
  "y": 1120
  g: 8ef86a1c4b59d8e1
  wires:
    - - a2334b77d3764815
- id: 8ef86a1c4b59d8e1
  type: group
  z: 292e70a6ba25b323
  name: Claim CAN source address
  x: 94
  "y": 799
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 07b8bcce03f19e8f
    - 1e244bad0304bb74
    - 3f5eb271f702c8f3
    - 4e46ba83df00a31b
    - 515386b6b0d01893
    - 55071115a3fe7c2c
    - 6423c433e099d2a5
    - 695ef0ed6d78a244
    - 73abad3deeed31e8
    - 8e350e2c0a0536b4
    - a2334b77d3764815
    - b846bc1605dae821
    - ba0c52ff61838986
    - d3cc901dd2fbd359
    - f01db5ebc3ae2e18
- id: 9cfcca5efde78052
  type: group
  z: 292e70a6ba25b323
  name: Load RV-C Dgn Names
  x: 994
  "y": 1379
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 74c7e4a6cc2508eb
    - 7e609140970f546c
    - c78420a075d43408
    - cd670f6465b6374b
- id: 9d8e7f6a5b4c3d2e
  type: function
  z: 292e70a6ba25b323
  name: publish_nodered_status
  func: |-
    // Publishes the retained online status after LibreCoach flows have loaded.
    // Input: startup inject (delayed so MQTT subscriptions are registered)
    // Output: retained message via "MQTT Out: Retain TRUE"
    return [[
      {
        topic: "librecoach/nodered/status",
        payload: "online",
      },
    ]];
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 450
  "y": 1380
  g: f81ca31c89243206
  wires:
    - - 0a63b31d61802934
- id: 9dd6a98018824be9
  type: group
  z: 292e70a6ba25b323
  name: Rest unique list
  x: 1694
  "y": 499
  g: f09f79586501cb0b
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 092c751dcdd12270
    - a4028527eb23fa04
    - c781b519a9f9b156
    - eea1641469efb23f
- id: 9e47041ed71c4494
  type: group
  z: 292e70a6ba25b323
  name: BETA Testing
  x: 994
  "y": 1739
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 027d238476871cfe
    - 6b1a7191643fa28e
    - d244b9fa7dfd5d48
    - e2fc2c67eba80f70
    - e38f32a1f6d36bfb
- id: 9e9b11b9441e51db
  type: mqtt in
  z: 292e70a6ba25b323
  name: HA in
  topic: homeassistant/climate/+/set_mode
  qos: "1"
  datatype: utf8
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 170
  "y": 340
  g: 3c819b7900145438
  wires:
    - - 64c5e5cfd4382453
      - d34a8c1fd2cc4320
- id: a1e89f48333c8256
  type: link in
  z: 292e70a6ba25b323
  name: Notify user
  links:
    - 47f3c921ae62126a
    - 47ac3adc971d73eb
    - cd73ee739dad9f59
    - fa104ff19bbee333
  x: 1080
  "y": 1300
  g: efe52fc701f550cf
  l: true
  wires:
    - - dd199275f909301f
- id: a2334b77d3764815
  type: link out
  z: 292e70a6ba25b323
  name: "MQTT out: Retain FALSE"
  mode: link
  links:
    - 74329f13cbbc528a
  x: 1680
  "y": 840
  g: 8ef86a1c4b59d8e1
  l: true
  wires: []
- id: a4028527eb23fa04
  type: inject
  z: 292e70a6ba25b323
  name: Reset unique
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: ""
  crontab: ""
  once: false
  onceDelay: 0.1
  topic: ""
  payload: ""
  payloadType: date
  x: 1810
  "y": 540
  g: 9dd6a98018824be9
  wires:
    - - 092c751dcdd12270
- id: a6594a9464d8fe4e
  type: switch
  z: 292e70a6ba25b323
  name: ""
  property: payload.dgn_name
  propertyType: msg
  rules:
    - t: cont
      v: AQUAHOT
      vt: str
    - t: cont
      v: STATUS
      vt: str
    - t: cont
      v: COMMAND
      vt: str
    - t: eq
      v: UNKNOWN
      vt: str
    - t: eq
      v: REQUEST_FOR_DGN
      vt: str
    - t: eq
      v: ADDRESS_CLAIMED
      vt: str
    - t: eq
      v: PROPRIETARY
      vt: str
  checkall: "false"
  repair: false
  outputs: 7
  x: 1470
  "y": 460
  g: f09f79586501cb0b
  wires:
    - - 398683928338f9c6
    - - 02bdfc59de71b5a5
      - 03a998942a940736
    - - 4fa9b9fdc36c83da
    - - c781b519a9f9b156
    - []
    - - 45414bacecada477
    - []
- id: a8a757fe3b31cf9c
  type: function
  z: 292e70a6ba25b323
  name: geo_status
  func: |
    // HA Status Publisher for Geo Bridge (can/status/geo)
    // Self-creating: publishes MQTT discovery on first valid reading.

    if (!global.get("geoEnabled")) return null;

    if (!msg.payload) {
      return null;
    }

    // In case the MQTT node isn't set to parse JSON automatically
    let p = msg.payload;
    if (typeof p === "string") {
      try {
        p = JSON.parse(p);
      } catch (e) {
        node.warn("Failed to parse geo payload as JSON");
        return null;
      }
    }

    if (p.status !== "online") {
      return null; // Ignore 'unavailable' or 'offline' status messages for now
    }

    // === Flow context: track which entities have had discovery published ===
    const CREATED_KEY = "geoSensorsCreated";
    let created = flow.get(CREATED_KEY);

    const messages = [];

    if (!created) {
      const device = {
        identifiers: ["librecoach-geo"],
        name: "Location",
        manufacturer: "LibreCoach",
      };

      const createSensor = (id, name, icon, unit) => {
        const entityId = `geo_${id}`;
        const payload = {
          name: name,
          unique_id: entityId,
          default_entity_id: `sensor.${entityId}`,
          icon: icon,
          state_topic: `homeassistant/sensor/${entityId}/state`,
          device: device,
        };
        if (unit) payload.unit_of_measurement = unit;

        messages.push({
          topic: `homeassistant/sensor/${entityId}/config`,
          payload: payload,
        });
      };

      createSensor("city", "City", "mdi:city");
      createSensor("state", "State", "mdi:map-marker");
      createSensor("timezone", "Timezone", "mdi:clock-outline");
      createSensor("elevation", "Elevation", "mdi:image-filter-hdr", "m");

      created = true;
      flow.set(CREATED_KEY, created);
    }

    // Publish states
    if (p.city !== undefined)
      messages.push({
        topic: `homeassistant/sensor/geo_city/state`,
        payload: p.city,
      });
    if (p.state !== undefined)
      messages.push({
        topic: `homeassistant/sensor/geo_state/state`,
        payload: p.state,
      });
    if (p.timezone !== undefined)
      messages.push({
        topic: `homeassistant/sensor/geo_timezone/state`,
        payload: p.timezone,
      });
    if (p.elevation !== undefined)
      messages.push({
        topic: `homeassistant/sensor/geo_elevation/state`,
        payload: p.elevation,
      });

    return [messages];
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 430
  "y": 1560
  g: f67b4a374db41b27
  wires:
    - - 5ffb64e684c20ea8
- id: aba7ef0da2fb2851
  type: function
  z: 292e70a6ba25b323
  name: discovery_index
  func: >
    // Indexes retained HA discovery configs by integration prefix so disable

    // handlers can delete an integration's entities.

    // Input: retained messages on homeassistant/+/+/config


    const PREFIXES = ["victron", "microair", "hughes"];


    const topic = msg.topic;

    if (typeof topic !== "string" || !topic.startsWith("homeassistant/")) return
    null;


    const index = global.get("discoveryIndex", "file") || {};


    // Empty payload = entity removed; drop the topic from every bucket.

    const raw = msg.payload;

    const isEmpty =
        raw === "" ||
        raw === null ||
        raw === undefined ||
        (typeof raw === "object" && Object.keys(raw).length === 0);

    if (isEmpty) {
        let changed = false;
        for (const p of PREFIXES) {
            const arr = index[p];
            if (arr) {
                const i = arr.indexOf(topic);
                if (i !== -1) {
                    arr.splice(i, 1);
                    changed = true;
                }
            }
        }
        if (changed) global.set("discoveryIndex", index, "file");
        return null;
    }


    // Parse the config payload to read unique_id.

    let cfg = raw;

    if (typeof raw === "string") {
        try {
            cfg = JSON.parse(raw);
        } catch (e) {
            return null;
        }
    }

    const uniqueId = cfg && cfg.unique_id;

    if (typeof uniqueId !== "string") return null;


    // Bucket by the integration prefix this entity belongs to.

    const prefix = PREFIXES.find((p) => uniqueId.startsWith(p + "_"));

    if (!prefix) return null;


    const bucket = index[prefix] || (index[prefix] = []);

    if (!bucket.includes(topic)) {
        bucket.push(topic);
        global.set("discoveryIndex", index, "file");
        node.status({ fill: "blue", shape: "dot", text: `${prefix}: ${bucket.length}` });
    }


    return null;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1360
  "y": 1660
  g: bc622f80fe99faa0
  wires:
    - []
- id: ac17a780a9a1dbcf
  type: link in
  z: 292e70a6ba25b323
  name: CONFIG_GLOBALS
  links:
    - 88e5fa035bedb48c
  x: 210
  "y": 1480
  g: f67b4a374db41b27
  l: true
  wires:
    - - 1becc1dacb0ecaf8
- id: b846bc1605dae821
  type: function
  z: 292e70a6ba25b323
  name: address_claim_request
  func: |
    // RV-C Address Claim - Send Initial Claim
    // Generates a persistent unique ID, selects an address, and broadcasts the
    // ADDRESS_CLAIMED message (DGN EE00h) to claim the address.

    // --- Step 1: Handle Manual Reset Command (if triggered) ---
    if (msg.topic === "reset_device_id") {
      // Clear the saved unique ID from persistent storage
      global.set("my_persistent_rvc_id", null, "file");
      // Reset the address attempt counter to start fresh
      context.set("address_attempt", null);
      node.warn(
        "Persistent DEVICE_NAME cleared. Generating a new one and starting claim...",
      );
      // By not returning, we allow the script to continue and generate a new ID immediately.
    }

    // --- Configuration ---
    const STARTING_ADDRESS = 223;
    const MIN_ADDRESS = 208;

    // --- Step 2: Create or Retrieve a Persistent Unique DEVICE_NAME ---
    let uniqueId = global.get("my_persistent_rvc_id", "file");

    if (!uniqueId) {
      // No ID found, so generate a new one.
      const randomSerial = Math.floor(Math.random() * 0x1fffff);
      const randomSerialHex = randomSerial
        .toString(16)
        .padStart(6, "0")
        .toUpperCase();
      uniqueId = "8000000000" + randomSerialHex;

      // Save this new ID to the global context for future use.
      global.set("my_persistent_rvc_id", uniqueId, "file");
      //node.warn("Generated and saved a new persistent DEVICE_NAME: " + uniqueId);
    }
    const DEVICE_NAME = uniqueId;

    // --- Step 3: Select an Address to Claim ---
    const usedAddresses = new Set(flow.get("used_addresses") || []);
    let addressToTry = context.get("address_attempt") || STARTING_ADDRESS;

    if (msg.topic === "address_claim_retry") {
      addressToTry--;
      node.warn(`Lost address conflict. Trying next address: ${addressToTry}`);
    }

    while (usedAddresses.has(addressToTry) && addressToTry >= MIN_ADDRESS) {
      addressToTry--;
      node.warn(
        `Address ${addressToTry + 1} is known to be in use, skipping to ${addressToTry}`,
      );
    }

    if (addressToTry < MIN_ADDRESS) {
      node.error(
        `Exhausted all addresses in the valid range (${STARTING_ADDRESS}-${MIN_ADDRESS}). Address claiming failed.`,
      );
      flow.set("claim_in_progress", false);
      return null;
    }

    context.set("address_attempt", addressToTry);

    // --- Step 4: Build and Send the Claim Message ---
    const DGN = "EE00";
    const priority = 6;
    const canIdNum = (priority << 26) | (parseInt(DGN, 16) << 8) | addressToTry;
    const canIdHex = canIdNum.toString(16).padStart(8, "0").toUpperCase();
    const data_payload = DEVICE_NAME;
    const canMessage = `${canIdHex}#${data_payload}`;

    flow.set("claiming_address", addressToTry);
    flow.set("claim_in_progress", true);
    flow.set("our_device_name", DEVICE_NAME);

    msg.payload = canMessage;
    msg.topic = "can/send";
    msg.claiming_address = addressToTry;

    node.log(
      `Attempting to claim address ${addressToTry} (0x${addressToTry.toString(16).toUpperCase()})`,
    );

    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 710
  "y": 840
  g: 8ef86a1c4b59d8e1
  wires:
    - - 55071115a3fe7c2c
      - a2334b77d3764815
- id: b86f82e84d961549
  type: mqtt in
  z: 292e70a6ba25b323
  name: HA in
  topic: homeassistant/+/+/set
  qos: "1"
  datatype: utf8
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 170
  "y": 280
  g: 3c819b7900145438
  wires:
    - - 64c5e5cfd4382453
      - d34a8c1fd2cc4320
- id: ba0c52ff61838986
  type: function
  z: 292e70a6ba25b323
  name: poll_autofill
  func: >
    // Polling Function for Autofill

    // Requests AUTOFILL_STATUS (1FFB1)


    // --- Configuration ---

    // The source address of this Node-RED system.

    const SOURCE_ADDRESS = global.get("rvc_source_address") || 254;

    const PRIORITY = 6;       // Standard priority for requests

    const DGN = "18EAFF";     // DGN for a broadcast request message


    // Build the data payload

    // This is the DGN for AUTOFILL_STATUS (1FFB1), sent in little-endian format
    (least-significant byte first).

    const data_payload = "B1FB01FFFFFFFF";


    // Construct the CAN ID

    const dgnInt = parseInt(DGN, 16);

    const canIdInt = (dgnInt << 8) | SOURCE_ADDRESS;

    const canIdHex = canIdInt.toString(16).padStart(8, '0');


    // Construct the final output message in CAN bus format

    msg.topic = "can/send";

    msg.payload = `${canIdHex.toUpperCase()}#${data_payload.toUpperCase()}`;


    //node.warn(`[Poll AutoFill] Sending global request for autofill status.
    CAN: ${msg.payload}`);


    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1390
  "y": 1080
  g: 8ef86a1c4b59d8e1
  wires:
    - - a2334b77d3764815
- id: bc622f80fe99faa0
  type: group
  z: 292e70a6ba25b323
  name: MQTT discovery
  x: 994
  "y": 1619
  style:
    stroke: "#777777"
    label: true
  nodes:
    - aba7ef0da2fb2851
    - e71f0cb356427a9c
- id: bce5d439a67df7da
  type: inject
  z: 292e70a6ba25b323
  name: Every 60s
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: "60"
  crontab: ""
  once: true
  onceDelay: "10"
  topic: ""
  payload: ""
  payloadType: date
  x: 1110
  "y": 1540
  g: 5169613bd2735138
  wires:
    - - dd22c9c987534342
- id: c2e1bc849357fcb4
  type: inject
  z: 292e70a6ba25b323
  name: Load on Start
  props:
    - p: payload
    - p: topic
      vt: str
  repeat: ""
  crontab: ""
  once: true
  onceDelay: "3"
  topic: ""
  payload: ""
  payloadType: date
  x: 220
  "y": 1340
  g: f81ca31c89243206
  wires:
    - - d6680567062a0908
- id: c781b519a9f9b156
  type: function
  z: 292e70a6ba25b323
  name: unique_unknown
  func: >
    // Unique Filter for Unknown Messages


    // Retrieve existing messages from flow context, or initialize as empty
    array

    let uniqueUnknown = flow.get("uniqueUnknown") || [];


    let newMsgStr = JSON.stringify(msg.payload.dgn);


    // Check if it's a new unique message

    if (!uniqueUnknown.includes(newMsgStr)) {
        uniqueUnknown.push(newMsgStr);
        flow.set("uniqueUnknown", uniqueUnknown);

        // Only return if new
        return msg;
    }


    // Return nothing if duplicate

    return null;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1810
  "y": 600
  g: 9dd6a98018824be9
  wires:
    - - eea1641469efb23f
- id: c78420a075d43408
  type: json
  z: 292e70a6ba25b323
  name: ""
  property: payload
  action: ""
  pretty: false
  x: 1530
  "y": 1420
  g: 9cfcca5efde78052
  wires:
    - - cd670f6465b6374b
- id: c83c6b2b70c927a3
  type: link out
  z: 292e70a6ba25b323
  name: "MQTT out: Retain FALSE"
  mode: link
  links:
    - 74329f13cbbc528a
  x: 1570
  "y": 1540
  g: 5169613bd2735138
  l: true
  wires: []
- id: c9d298bbaa855984
  type: function
  z: 292e70a6ba25b323
  name: store_config_globals
  func: |
    const key = msg.topic.split("/").pop();
    const val = msg.payload.toString() === "true";

    if (key === "victron_enabled") global.set("victronEnabled", val);
    if (key === "beta_enabled") global.set("betaEnabled", val);
    if (key === "microair_enabled") global.set("microairEnabled", val);
    if (key === "hughes_enabled") global.set("hughesEnabled", val);
    if (key === "geo_enabled") global.set("geoEnabled", val);
    if (key === "rvc_time_sync_enabled") global.set("timeSyncEnabled", val);

    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 380
  "y": 680
  g: 3c819b7900145438
  wires:
    - - 88e5fa035bedb48c
- id: cacf35b859af22a9
  type: mqtt out
  z: 292e70a6ba25b323
  name: "MQTT Out: Retain TRUE"
  topic: ""
  qos: "0"
  retain: "true"
  respTopic: ""
  contentType: ""
  userProps: ""
  correl: ""
  expiry: ""
  broker: 80727e60a251c36c
  x: 630
  "y": 500
  g: 3c819b7900145438
  wires: []
- id: cd670f6465b6374b
  type: function
  z: 292e70a6ba25b323
  name: store_dgn_map
  func: |
    // Convert the data to a Map when initially storing it
    const dgnMap = new Map(
      msg.payload.map((entry) => [entry.Hex.toString().toUpperCase(), entry.DGN]),
    );

    // Store the Map in global context
    global.set("dgnMap", dgnMap);

    // Count the number of records
    const decoderCount = dgnMap.size;

    // Update the node status to indicate success
    node.status({
      fill: "green",
      shape: "dot",
      text: "Reference loaded",
    });
    return null;
  outputs: 0
  timeout: ""
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1740
  "y": 1420
  g: 9cfcca5efde78052
  wires: []
- id: cd73ee739dad9f59
  type: link out
  z: 292e70a6ba25b323
  name: Notify user
  mode: link
  links:
    - a1e89f48333c8256
  x: 690
  "y": 1840
  g: 1250cab4e8633d65
  l: true
  wires: []
- id: ceee39e4b10d6030
  type: group
  z: 292e70a6ba25b323
  x: 874
  "y": 559
  g: f09f79586501cb0b
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 03a998942a940736
    - 3d07bdca6b71b669
    - 8037e09fbf4ac87b
- id: cfa55144f4a17a5c
  type: file
  z: 292e70a6ba25b323
  name: Write file
  filename: filename
  filenameType: msg
  appendNewline: true
  createDir: true
  overwriteFile: "true"
  encoding: none
  x: 680
  "y": 1800
  g: 1250cab4e8633d65
  wires:
    - []
- id: d244b9fa7dfd5d48
  type: link in
  z: 292e70a6ba25b323
  name: BETA TEST
  links: []
  x: 1090
  "y": 1840
  g: 9e47041ed71c4494
  l: true
  wires:
    - - 6b1a7191643fa28e
- id: d34a8c1fd2cc4320
  type: debug
  z: 292e70a6ba25b323
  name: HA in
  active: false
  tosidebar: true
  console: false
  tostatus: false
  complete: payload
  targetType: msg
  statusVal: ""
  statusType: auto
  x: 570
  "y": 320
  g: 3c819b7900145438
  wires: []
- id: d3cc901dd2fbd359
  type: function
  z: 292e70a6ba25b323
  name: poll_water_pump
  func: >
    // Polling Function for Water Pump

    // Requests WATER_PUMP_STATUS (1FFB3)


    // --- Configuration ---

    // The source address of this Node-RED system.

    const SOURCE_ADDRESS = global.get("rvc_source_address") || 254;

    const PRIORITY = 6;       // Standard priority for requests

    const DGN = "18EAFF";     // DGN for a broadcast request message


    // Build the data payload

    // This is the DGN for WATER_PUMP_STATUS (1FFB3), sent in little-endian
    format (least-significant byte first).

    const data_payload = "B3FB01FFFFFFFF";


    // Construct the CAN ID

    const dgnInt = parseInt(DGN, 16);

    const canIdInt = (dgnInt << 8) | SOURCE_ADDRESS;

    const canIdHex = canIdInt.toString(16).padStart(8, '0');


    // Construct the final output message in CAN bus format

    msg.topic = "can/send";

    msg.payload = `${canIdHex.toUpperCase()}#${data_payload.toUpperCase()}`;


    //node.warn(`[Poll Water Pump] Sending global request for water pump status.
    CAN: ${msg.payload}`);


    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1410
  "y": 1040
  g: 8ef86a1c4b59d8e1
  wires:
    - - a2334b77d3764815
- id: d6680567062a0908
  type: function
  z: 292e70a6ba25b323
  name: create_user_toggles
  func: >-
    // Creates LibreCoach: System config toggles via MQTT Discovery


    const device = {
      identifiers: ["librecoach-system"],
      name: "LibreCoach: System",
      manufacturer: "LibreCoach",
    };


    const NODE_RED_AVAILABILITY_TOPIC = "librecoach/nodered/status";


    const messages = [];


    // === Import/Export & RV Info ===


    // Export Configuration button

    messages.push({
      topic: "homeassistant/button/librecoach_export_config/config",
      payload: {
        name: "Export LibreCoach Configuration",
        unique_id: "librecoach_export_config",
        default_entity_id: "button.librecoach_export_config",
        icon: "mdi:download",
        command_topic: "librecoach/export/trigger",
        device: device,
      },
    });


    // Import Configuration button

    messages.push({
      topic: "homeassistant/button/librecoach_import_config/config",
      payload: {
        name: "Import LibreCoach Configuration",
        unique_id: "librecoach_import_config",
        default_entity_id: "button.librecoach_import_config",
        icon: "mdi:upload",
        command_topic: "librecoach/import/trigger",
        device: device,
      },
    });


    // RV Info fields

    const rvFields = [
      {
        id: "rv_manufacturer",
        name: "RV Manufacturer",
        icon: "mdi:factory",
        max: 50,
      },
      { id: "rv_model", name: "RV Model", icon: "mdi:rv-truck", max: 50 },
      { id: "rv_year", name: "RV Year", icon: "mdi:calendar", max: 4 },
      { id: "rv_other", name: "RV Other", icon: "mdi:note-text", max: 100 },
    ];


    for (const field of rvFields) {
      // Config
      messages.push({
        topic: `homeassistant/text/${field.id}/config`,
        payload: {
          name: field.name,
          unique_id: field.id,
          default_entity_id: `text.${field.id}`,
          icon: field.icon,
          state_topic: `librecoach/rv_info/${field.id}`,
          command_topic: `librecoach/rv_info/${field.id}/set`,
          max: field.max,
          device: device,
        },
      });

      // State (restore from global or empty string)
      const storedValue = global.get(field.id, "file") || "";
      messages.push({
        topic: `librecoach/rv_info/${field.id}`,
        payload: storedValue,
        retain: true,
      });
    }


    // === Unknown CAN Recording ===


    // Switch - Record Unknown CAN

    messages.push({
      topic: "homeassistant/switch/librecoach_record_unknown/config",
      payload: {
        name: "Tools: Record Unknown Commands",
        unique_id: "librecoach_record_unknown",
        default_entity_id: "switch.librecoach_record_unknown",
        icon: "mdi:record-rec",
        command_topic: "librecoach/record_unknown/set",
        state_topic: "homeassistant/switch/librecoach_record_unknown/state",
        payload_on: "1",
        payload_off: "0",
        state_on: "ON",
        state_off: "OFF",
        optimistic: false,
        value_template: "{{ 'ON' if value == '1' else 'OFF' }}",
        device: device,
      },
    });


    // State - Record Unknown CAN default off

    global.set("recordUnknown", false, "file");

    messages.push({
      topic: "homeassistant/switch/librecoach_record_unknown/state",
      payload: 0,
      retain: true,
    });


    // Export AI Dashboard Prompt button (mushroom/custom cards)

    messages.push({
      topic: "homeassistant/button/librecoach_export_entities/config",
      payload: {
        name: "Export AI Dashboard Prompt (Mushroom Cards)",
        unique_id: "librecoach_export_entities",
        default_entity_id: "button.librecoach_export_entities",
        icon: "mdi:robot-outline",
        command_topic: "librecoach/export/entities/trigger",
        device: device,
      },
    });


    // Export AI Dashboard Prompt button (standard HA cards only — no HACS
    needed)

    messages.push({
      topic: "homeassistant/button/librecoach_export_entities_default/config",
      payload: {
        name: "Export AI Dashboard Prompt (Standard Cards)",
        unique_id: "librecoach_export_entities_default",
        default_entity_id: "button.librecoach_export_entities_default",
        icon: "mdi:view-dashboard-outline",
        command_topic: "librecoach/export/entities/default/trigger",
        device: device,
      },
    });


    // Bluetooth device reset tool

    messages.push({
      topic: "homeassistant/button/librecoach_forget_bluetooth/config",
      payload: {
        name: "Forget Bluetooth Devices",
        unique_id: "librecoach_forget_bluetooth",
        default_entity_id: "button.librecoach_forget_bluetooth",
        icon: "mdi:bluetooth-off",
        command_topic: "librecoach/ble/reset_locks",
        device: device,
      },
    });


    // Button - Export Unknown Recording

    messages.push({
      topic: "homeassistant/button/librecoach_export_unknown/config",
      payload: {
        name: "Tools: Save Recorded Log",
        unique_id: "librecoach_export_unknown",
        default_entity_id: "button.librecoach_export_unknown",
        icon: "mdi:download",
        command_topic: "librecoach/export/unknown/trigger",
        device: device,
      },
    });


    // === Cleanup Old Entities ===

    // Send empty payload to delete orphaned entities from Home Assistant


    const cleanupTopics = [
      "homeassistant/button/librecoach_ble_reset_locks/config",
      "homeassistant/switch/victron_integration/config",
      "homeassistant/switch/librecoach_testing_beta/config",
      "homeassistant/switch/librecoach_beta_testing/config",
    ];


    for (const topic of cleanupTopics) {
      messages.push({ topic: topic, payload: "" });
    }


    for (const message of messages) {
      if (
        message.topic &&
        message.topic.startsWith("homeassistant/") &&
        message.payload &&
        typeof message.payload === "object" &&
        !message.payload.availability_topic
      ) {
        message.payload.availability_topic = NODE_RED_AVAILABILITY_TOPIC;
      }
    }


    node.status({
      fill: "green",
      shape: "dot",
      text: `System Entities Created`,
    });


    return [messages];
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 440
  "y": 1300
  g: f81ca31c89243206
  wires:
    - - 0a63b31d61802934
- id: d79041ca83ce8b8c
  type: debug
  z: 292e70a6ba25b323
  name: Recorded message
  active: false
  tosidebar: true
  console: false
  tostatus: false
  complete: payload
  targetType: msg
  statusVal: ""
  statusType: auto
  x: 1790
  "y": 320
  g: f09f79586501cb0b
  wires: []
- id: dca8346e061f1d42
  type: link out
  z: 292e70a6ba25b323
  name: "MQTT out: Retain TRUE"
  mode: link
  links:
    - f658418a7b9b4857
  x: 730
  "y": 1700
  g: 1250cab4e8633d65
  l: true
  wires: []
- id: dd199275f909301f
  type: function
  z: 292e70a6ba25b323
  name: notify_user
  func: |
    // Centralized Notification Handler
    // Input: msg.payload with { title, message, notification_id }
    // Output: msg configured for HTTP Request to HA notification service

    const haBaseUrl = "http://supervisor/core";
    const haToken = env.get("SUPERVISOR_TOKEN");

    if (!haToken) {
        node.warn("No Supervisor token - skipping notification");
        return null;
    }

    const payload = msg.payload || {};

    if (!payload.title || !payload.message) {
        node.warn("Notification missing title or message");
        return null;
    }

    msg.url = `${haBaseUrl}/api/services/persistent_notification/create`;
    msg.method = "POST";
    msg.headers = {
        Authorization: `Bearer ${haToken}`,
        "Content-Type": "application/json",
    };

    msg.payload = {
        title: payload.title,
        message: payload.message,
        notification_id: payload.notification_id || "librecoach_notification",
    };

    return msg;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1310
  "y": 1300
  g: efe52fc701f550cf
  wires:
    - - 8337a7f0dd0435d6
- id: dd22c9c987534342
  type: function
  z: 292e70a6ba25b323
  name: rvc_time_sync
  func: >
    // Broadcasts DATE_TIME_STATUS to the RV-C network (DGN 1FFFFh, §6.4)

    // Fires every 60s via inject node. Gated by timeSyncEnabled global
    (default: false).

    // Note: spec nominal interval is 1000 ms; 60s is adequate for clock
    correction without

    // competing with a hardware master. No source-address arbitration is
    implemented here.


    // --- Configuration ---

    const PRIORITY = 6;

    const DGN = 0x1ffff;

    const SOURCE_ADDRESS = global.get("rvc_source_address") || 254;


    // --- Feature Gate ---

    const timeSyncEnabled = global.get("timeSyncEnabled");

    if (!timeSyncEnabled) return null;


    // --- Current Time ---

    const dt = new Date();


    // --- Helpers ---

    // RV-C DOW: 1=Sunday..7=Saturday (JS getDay(): 0=Sun..6=Sat)

    const rvcDow = (jsDow) => (jsDow === 0 ? 1 : jsDow + 1);


    // Derive TZ code from system UTC offset: std offset in hours, minus 1 if
    DST active

    const jan = new Date(dt.getFullYear(), 0, 1).getTimezoneOffset();

    const jul = new Date(dt.getFullYear(), 6, 1).getTimezoneOffset();

    const std = Math.max(jan, jul);

    const isDst = dt.getTimezoneOffset() < std;

    const tzCode = isDst ? std / 60 - 1 : std / 60;


    // --- Payload ---

    // Byte layout per RV-C §6.4 Table 6.4.2b: year(offset 2000), month, day,
    dow, hour, min, sec, tz

    const dataBytes = [
      Math.max(0, Math.min(255, dt.getFullYear() - 2000)), // Byte 0: Year (2000..2255)
      dt.getMonth() + 1, // Byte 1: Month (1..12)
      dt.getDate(), // Byte 2: Day (1..31)
      rvcDow(dt.getDay()), // Byte 3: Day of week (1..7)
      dt.getHours(), // Byte 4: Hour (0..23)
      dt.getMinutes(), // Byte 5: Minute (0..59)
      dt.getSeconds(), // Byte 6: Second (0..59)
      tzCode & 0xff, // Byte 7: Timezone code
    ];


    // --- Build and Send ---

    const dataHex = dataBytes
      .map((b) => b.toString(16).padStart(2, "0"))
      .join("")
      .toUpperCase();
    const canIdInt = (PRIORITY << 26) | (DGN << 8) | SOURCE_ADDRESS;

    const canIdHex = canIdInt.toString(16).padStart(8, "0").toUpperCase();


    node.status({
      fill: "green",
      shape: "dot",
      text: `${dt.getMonth() + 1}/${dt.getDate()} ${dt.getHours().toString().padStart(2, "0")}:${dt.getMinutes().toString().padStart(2, "0")}:${dt.getSeconds().toString().padStart(2, "0")} tz=${tzCode}`,
    });


    return { topic: "can/send", payload: `${canIdHex}#${dataHex}` };
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1320
  "y": 1540
  g: 5169613bd2735138
  wires:
    - - c83c6b2b70c927a3
- id: e2fc2c67eba80f70
  type: function
  z: 292e70a6ba25b323
  d: true
  name: beta_gate
  func: >
    // A simple gate node to allow messages to pass only if Beta features are
    enabled.


    // Check the global betaEnabled flag

    const isBetaEnabled = global.get("betaEnabled");


    // If beta is enabled, pass the message through unmodified.

    // Otherwise, return null to drop the message and stop the flow along this
    path.

    if (isBetaEnabled === true) {
      return msg;
    }


    return null;
  outputs: 1
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 1420
  "y": 1840
  g: 9e47041ed71c4494
  wires:
    - []
- id: e38f32a1f6d36bfb
  type: link out
  z: 292e70a6ba25b323
  name: "MQTT out: Retain TRUE"
  mode: link
  links:
    - f658418a7b9b4857
  x: 1670
  "y": 1780
  g: 9e47041ed71c4494
  l: true
  wires: []
- id: e71f0cb356427a9c
  type: mqtt in
  z: 292e70a6ba25b323
  name: HA discovery configs
  topic: homeassistant/+/+/config
  qos: "1"
  datatype: auto
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 1120
  "y": 1660
  g: bc622f80fe99faa0
  wires:
    - - aba7ef0da2fb2851
- id: eea1641469efb23f
  type: debug
  z: 292e70a6ba25b323
  name: New Unknown DGN
  active: false
  tosidebar: true
  console: false
  tostatus: false
  complete: payload
  targetType: msg
  statusVal: ""
  statusType: auto
  x: 2100
  "y": 600
  g: 9dd6a98018824be9
  wires: []
- id: efe52fc701f550cf
  type: group
  z: 292e70a6ba25b323
  name: Notifications
  x: 994
  "y": 1259
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 8337a7f0dd0435d6
    - a1e89f48333c8256
    - dd199275f909301f
- id: f01db5ebc3ae2e18
  type: link in
  z: 292e70a6ba25b323
  name: ADDRESS_CLAIMED
  links:
    - 45414bacecada477
  x: 220
  "y": 920
  g: 8ef86a1c4b59d8e1
  l: true
  wires:
    - - 4e46ba83df00a31b
- id: f09f79586501cb0b
  type: group
  z: 292e70a6ba25b323
  name: CAN message routing
  x: 848
  "y": 239
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 02bdfc59de71b5a5
    - 0c76b4a89a0e60ca
    - 2110ea4d0b85f784
    - 21dfe84b8adafa6e
    - 3935564c9401e95d
    - 398683928338f9c6
    - 45414bacecada477
    - 4fa9b9fdc36c83da
    - 52f6bcc4fb0ca263
    - 6376d98589368f2b
    - 8d7d5d3ea117cf79
    - 9dd6a98018824be9
    - a6594a9464d8fe4e
    - ceee39e4b10d6030
    - d79041ca83ce8b8c
- id: f3ee6dc19b0155a0
  type: function
  z: 292e70a6ba25b323
  name: record_unknown_export
  func: >
    // Export Unknown CAN Recording

    // Reads recordUnknownLog from global context

    // Output: [[jsonMsg, htmlMsg], notifyMsg] to File Write node


    const TIMEZONE = "America/New_York";


    function getLocalISO(ms) {
      if (!ms) return null;
      return new Date(ms)
        .toLocaleString("sv-SE", { timeZone: TIMEZONE, hour12: false })
        .replace(" ", "T");
    }


    const log = global.get("recordUnknownLog", "file") || [];

    const startTime = global.get("recordUnknownStart", "file");


    const report = {
      recording_start: getLocalISO(startTime),
      recording_end: log.length > 0 ? log[log.length - 1].timestamp : null,
      message_count: log.length,
      messages: log,
    };


    // Static filename replaces old files every time

    const filename = "librecoach_unknown_recording.json";


    const jsonMsg = {
      filename: `/homeassistant/www/${filename}`,
      payload: JSON.stringify(report, null, 2),
    };


    const htmlMsg = {
      filename: "/homeassistant/www/librecoach_download_recording.html",
      payload: `<!DOCTYPE html>
    <html><head><title>LibreCoach Download</title></head>

    <body>

    <script>

    var a = document.createElement("a");

    a.href = "/local/${filename}";

    a.download = "${filename}";

    document.body.appendChild(a);

    a.click();

    setTimeout(function() { window.close(); }, 1000);

    </script>

    <p>Your download should start automatically. If not, <a
    href="/local/${filename}" download="${filename}">click here</a>.</p>

    </body></html>`,

    };


    const notifyMsg = {
      payload: {
        title: "LibreCoach Unknown Recording",
        message: `Your recording is ready.<br><a href="/local/librecoach_download_recording.html" target="_blank"><b>Click here to download</b></a>.`,
        notification_id: "librecoach_export_unknown",
      },
    };


    node.status({
      fill: "green",
      shape: "dot",
      text: `Exported ${log.length} messages`,
    });


    return [[jsonMsg, htmlMsg], notifyMsg];
  outputs: 2
  timeout: 0
  noerr: 0
  initialize: ""
  finalize: ""
  libs: []
  x: 450
  "y": 1820
  g: 1250cab4e8633d65
  wires:
    - - cfa55144f4a17a5c
    - - cd73ee739dad9f59
- id: f658418a7b9b4857
  type: link in
  z: 292e70a6ba25b323
  name: "MQTT out: Retain TRUE"
  links:
    - d38c75bc1474be23
    - d3a13b531babee66
    - 1c1403c14b577fb4
    - 12a57f6a07d0bf1a
    - bd38c37850a3e832
    - 0a63b31d61802934
    - 950e9ce654224317
    - f3f99ba42088c9b1
    - 599ffbccfb7cd415
    - b104a7e82aefcb21
    - dca8346e061f1d42
    - e371075d4dbfc113
    - e38f32a1f6d36bfb
    - 102272ba5b34f5e1
    - 6376d98589368f2b
    - 5ffb64e684c20ea8
  x: 230
  "y": 500
  g: 3c819b7900145438
  l: true
  wires:
    - - cacf35b859af22a9
- id: f67b4a374db41b27
  type: group
  z: 292e70a6ba25b323
  name: GPS Updates
  x: 94
  "y": 1439
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 1becc1dacb0ecaf8
    - 50dfcea54d80866b
    - 5ffb64e684c20ea8
    - a8a757fe3b31cf9c
    - ac17a780a9a1dbcf
- id: f81ca31c89243206
  type: group
  z: 292e70a6ba25b323
  name: User input
  x: 94
  "y": 1259
  style:
    stroke: "#777777"
    label: true
  nodes:
    - 012b02175347902e
    - 0a63b31d61802934
    - 9d8e7f6a5b4c3d2e
    - c2e1bc849357fcb4
    - d6680567062a0908
- id: ff7901a611c1e1aa
  type: mqtt in
  z: 292e70a6ba25b323
  name: Recording toggle
  topic: librecoach/record_unknown/set
  qos: "1"
  datatype: utf8
  broker: 80727e60a251c36c
  nl: false
  rap: true
  rh: 0
  inputs: 0
  x: 200
  "y": 1700
  g: 1250cab4e8633d65
  wires:
    - - 880ca1b6bf10b8d2
